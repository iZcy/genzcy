# PromptAudit — Commit & Push Workflow

After every completed task that modifies files:
1. Run `git add -A`
2. Run `git commit -m "type: concise description of what was changed"`
3. Run `git push`

Always use descriptive commit messages. Never leave uncommitted changes at the end of a task. Push every commit so your mentor can track progress.
